import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faPlay} from "@fortawesome/free-solid-svg-icons";

const playIcon = <FontAwesomeIcon icon={faPlay} />

const itemiconmusic = {
    play : playIcon
}

export default itemiconmusic;

